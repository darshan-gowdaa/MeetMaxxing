{
  "name": "meetmaxxing",
  "version": "1.0.0",
  "description": "MeetMaxxing — Enterprise AI Meeting Copilot monorepo",
  "private": true,
  "scripts": {
    "postinstall": "node scripts/install.js",
    "install:all": "node scripts/install.js",
    "dev": "concurrently --kill-others --prefix-colors \"blue,green\" --names \"Backend,Frontend\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && uv run python -m uvicorn main:app --reload --port 8000",
    "dev:frontend": "cd frontend && npm run dev",
    "build:backend": "cd backend && uv run ruff check .",
    "build": "npm run build:backend && cd frontend && npm run build",
    "lint": "cd frontend && npm run lint",
    "kill": "npx kill-port 3000 8000 50051",
    "restart": "npm run kill && npm run dev",
    "start": "node scripts/start.js",
    "start:backend": "cd backend && uv run python -m uvicorn main:app --port 8000",
    "start:frontend": "cd frontend && npm run start"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
